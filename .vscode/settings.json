{
    "docker.commands.composeUp": [
        {
            "label": "override",
            "template": "docker-compose -f src/docker-compose.yml -f src/docker-compose.override.yml up -d --build"
        }
    ]
}
